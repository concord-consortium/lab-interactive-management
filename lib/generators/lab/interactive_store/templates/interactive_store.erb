# Generated by $ rails generate interactive_store on <%=Time.now.strftime('%e %b %Y %H:%m:%S%p') %>
# Depends on the definition of interactive metadata in the lab project

# just use this in the Interactive AR model
module InteractiveStore
  extend ActiveSupport::Concern

  module ClassMethods
  end

  module InstanceMethods
  end

  included do
      <%= config[:serialize_str] %>
      <%= config[:store_str] %>
  end
end
